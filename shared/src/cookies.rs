/// The cookie to store the session token.
pub const SESSION_COOKIE: &str = "session";

/// The cookie to store the provider of the authentication.
pub const AUTH_PROVIDER_COOKIE: &str = "auth-provider";

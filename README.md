# TaskNet

## Run locally

```shell
cd web && trunk serve
```

## Run the server

```shell
make run
```

### Server configuration

A template configuration file is provided in `config-template.json`.
